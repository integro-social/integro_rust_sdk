// This file was generated by tapir. Do not edit this file manually.
#![allow(unused_imports, non_snake_case, non_camel_case_types, clippy::all)]
use crate::types::meta::whatsapp_template_button::WhatsappTemplateButton;
use crate::types::meta::whatsapp_template_header::WhatsappTemplateHeader;

/// One block of a template *definition* — the shape Meta stores for an
/// approved template and the shape a create request submits.
///
/// This crate's own vocabulary, snake_case like every other enum on the surface;
/// Meta spells these uppercase, and that spelling lives at the boundary rather
/// than on the contract.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[cfg_attr(feature = "tapir", derive(tapir::Reflect), tapir(namespace = "integro_sdk.meta"))]
#[serde(tag = "type")]
#[serde(rename_all = "snake_case")]
pub enum WhatsappTemplateComponent {
  /// The optional block above the body. What it carries depends on its format.
  Header {
    #[serde(flatten)]
    format: WhatsappTemplateHeader,
  },
  /// The message text — the only required block. `{{1}}`-style placeholders
  /// are substituted at send time; `example` gives Meta's reviewers one sample
  /// per placeholder, in order.
  Body {
    text: String,
    example: Option<Vec<String>>,
  },
  /// A short trailing line. Never carries placeholders.
  Footer {
    text: String,
  },
  Buttons {
    buttons: Vec<WhatsappTemplateButton>,
  },
  /// A block kind this crate does not model — carousels, limited-time offers,
  /// authentication add-ons. Read-only: Meta returns it and it is surfaced
  /// so the template still lists, but a create request carrying one drops
  /// the block (Meta then rejects a template that lost a required part).
  Unsupported,
}
