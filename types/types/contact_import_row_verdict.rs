// This file was generated by tapir. Do not edit this file manually.
#![allow(unused_imports, non_snake_case, non_camel_case_types, clippy::all)]

/// One row's outcome, on two deliberately separate axes. The parse verdicts
/// (`MissingPhone`, `InvalidPhone`, `InvalidName`, `DuplicateInFile`) are
/// final at upload and editable while the import is staged; the platform
/// verdicts (`Imported`, `NotOnWhatsapp`, `CheckFailed`) are the loader's.
/// `CheckFailed` is distinct from `NotOnWhatsapp` on purpose: one is an
/// answer, the other is the absence of one.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[cfg_attr(feature = "tapir", derive(tapir::Reflect), tapir(namespace = "integro_sdk.types"))]
#[serde(rename_all = "snake_case")]
pub enum ContactImportRowVerdict {
  /// Parsed fine; waiting for the load phase.
  Pending,
  /// The mapped phone column was empty.
  MissingPhone,
  /// Digits no country claims, or that its numbering plan rejects.
  InvalidPhone,
  /// The mapped name cell fails the `Name` rules (2..=100 chars, no control
  /// characters). An empty cell is valid — the alias just stays unset.
  InvalidName,
  /// The same number appeared earlier in this same file.
  DuplicateInFile,
  /// The conversation exists — created by this import, or already known.
  Imported,
  /// Checked on the platform: the number is not on WhatsApp.
  NotOnWhatsapp,
  /// The check itself failed (session down, gateway error); retryable.
  CheckFailed,
  /// Canceled with the import before the loader reached it. Terminal.
  Skipped,
}
