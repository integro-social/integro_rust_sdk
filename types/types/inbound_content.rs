// This file was generated by tapir. Do not edit this file manually.
#![allow(unused_imports, non_snake_case, non_camel_case_types, clippy::all)]

/// Content that only arrives inbound. There is no send call for these shapes
/// — a send request takes [`OutboundContent`] and cannot even express them.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[cfg_attr(feature = "tapir", derive(tapir::Reflect), tapir(namespace = "integro_sdk.types"))]
#[serde(tag = "kind")]
#[serde(rename_all = "snake_case")]
pub enum InboundContent {
  /// A shared post/reel/link.
  Share {
    url: String,
  },
  /// The user mentioned the account in their story.
  StoryMention {
    url: String,
  },
  /// A reply to one of the account's stories; `story_url` is the story being
  /// replied to.
  StoryReply {
    story_url: String,
    text: String,
  },
  /// The participant tapped one of the quick replies attached to a previous
  /// message (or a whatsapp interactive button/list row).
  QuickReply {
    payload: String,
    text: String,
  },
  /// The participant tapped a postback button from a template.
  Postback {
    title: String,
    payload: String,
  },
  /// The platform withheld the content itself — view-once media never
  /// reaches companion devices, and some ciphertexts stay undecryptable.
  /// Registered so operators see the contact sent something; `reason` is the
  /// platform's label when it names one (`view_once`).
  Unavailable {
    reason: Option<String>,
  },
  /// Something the hub does not model yet. Stored so the conversation stays
  /// complete.
  Unsupported,
}
