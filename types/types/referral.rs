// This file was generated by tapir. Do not edit this file manually.
#![allow(unused_imports, non_snake_case, non_camel_case_types, clippy::all)]

/// Where a conversation (re-)entry came from, normalized across every channel
/// that reports one. Kind-tagged so consumers distinguish origins without
/// probing fields: each variant carries exactly what its origin provides.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[cfg_attr(feature = "tapir", derive(tapir::Reflect), tapir(namespace = "integro_sdk.types"))]
#[serde(tag = "kind")]
#[serde(rename_all = "snake_case")]
pub enum Referral {
  /// A click-to-WhatsApp ad (or boosted post). The official Cloud API sends
  /// it as the message's `referral` object; the session-backed flavors carry
  /// the same facts as `contextInfo.externalAdReply` in the message proto.
  /// `ctwa_clid` is the load-bearing field — Meta's Conversions API takes it
  /// back to attribute a sale to the ad.
  WhatsappAd {
    /// Link to the ad or post.
    source_url: Option<String>,
    /// The ad or post id — what campaign reporting groups by.
    source_id: Option<String>,
    /// `ad` or `post`.
    source_type: Option<String>,
    /// The creative's title.
    headline: Option<String>,
    /// The creative's description.
    body: Option<String>,
    /// `image` or `video`.
    media_type: Option<String>,
    /// The creative's image, video still, or thumbnail — whichever the
    /// channel reported, collapsed to one url.
    thumbnail_url: Option<String>,
    /// The click token, unique per ad tap.
    ctwa_clid: Option<String>,
  },
  /// A click-to-messenger / click-to-instagram-direct ad. No click token —
  /// Meta attributes these conversions on its own side.
  MessengerAd {
    /// The ad id — what campaign reporting groups by.
    ad_id: Option<String>,
    /// The creative's title.
    headline: Option<String>,
    /// The creative's image or video still, collapsed to one url.
    thumbnail_url: Option<String>,
    post_id: Option<String>,
    product_id: Option<String>,
    /// The ref payload set on the ad, when one was.
    ref_payload: Option<String>,
  },
  /// A tracked non-ad origin: an m.me/ig.me ref link, a messenger code, or
  /// the customer chat plugin.
  Link {
    /// `SHORTLINK`, `MESSENGER_CODE`, `CUSTOMER_CHAT_PLUGIN`, …
    source: Option<String>,
    /// The ref payload the link carried.
    ref_payload: Option<String>,
    /// The page the chat plugin was embedded on.
    referer_uri: Option<String>,
  },
}
