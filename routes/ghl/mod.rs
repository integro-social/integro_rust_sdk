// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::types::ghl::connect_ghl_request::ConnectGhlRequest;
use crate::types::ghl::connect_ghl_response::ConnectGhlResponse;
use crate::types::ghl::ghl_callback_query::GhlCallbackQuery;
use crate::types::database::ghl_location::GhlLocation;
use crate::types::ghl::link_ghl_request::LinkGhlRequest;
use crate::types::ghl::list_ghl_query::ListGhlQuery;

/// GoHighLevel OAuth redirect target: consumes the state, exchanges the code
/// and registers (or reconnects) the location; always redirects the browser
/// back to the frontend, carrying an error slug when the install failed.
///
/// Public — no authentication required; authorization comes from the one-shot state token issued by `ghl.connect`.
pub async fn callback(__client: &crate::runtime::Client, __query: &GhlCallbackQuery) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/ghl/callback");
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Start the GoHighLevel OAuth flow for a group, returning the location-choice
/// dialog URL; the location the admin picks there is connected to the group by
/// the callback.
///
/// Requires `ConnectSocialAccounts` in the named group.
pub async fn connect(__client: &crate::runtime::Client, __body: &ConnectGhlRequest) -> crate::runtime::ApiResult<ConnectGhlResponse> {
  let mut __path = String::from("/ghl/connect");
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Disconnect the location, deleting its stored grant and its binding.
/// Reinstalling the app from GoHighLevel is what brings it back.
///
/// Requires `ConnectSocialAccounts` in the location's group.
pub async fn disconnect(__client: &crate::runtime::Client, ghl_location_uid: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/ghl/{ghl_location_uid}");
  __path = __path.replace("{ghl_location_uid}", &crate::runtime::encode_path(ghl_location_uid));
  __client.request(crate::runtime::Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// Bridge a whatsapp account into the location's conversations inbox: what the
/// location sends over the provider goes out on that account, and what the
/// account receives is mirrored back. The body is channel-tagged — only the
/// whatsapp flavors that can open a conversation from a phone number
/// deserialize. Only history from this moment on is carried over — the sync
/// starts at the current message head.
///
/// Requires `ConnectSocialAccounts` in the location's group — the group the target account must belong to as well.
pub async fn link(__client: &crate::runtime::Client, ghl_location_uid: &str, __body: &LinkGhlRequest) -> crate::runtime::ApiResult<GhlLocation> {
  let mut __path = String::from("/ghl/{ghl_location_uid}/link");
  __path = __path.replace("{ghl_location_uid}", &crate::runtime::encode_path(ghl_location_uid));
  __client.request(crate::runtime::Method::PUT, &__path, None::<&()>, Some(__body)).await
}
/// List the GoHighLevel locations installed in a group, each carrying the
/// whatsapp account it bridges (`target_account_uid`, null while unlinked) and
/// whether its grant needs reconnecting.
///
/// Requires `ViewSocialAccounts` in the named group.
pub async fn list(__client: &crate::runtime::Client, __query: &ListGhlQuery) -> crate::runtime::ApiResult<Vec<GhlLocation>> {
  let mut __path = String::from("/ghl");
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Unbridge the location's whatsapp account, stopping the mirroring in both
/// directions. The location stays installed and can be pointed at another
/// account.
///
/// Requires `ConnectSocialAccounts` in the location's group.
pub async fn unlink(__client: &crate::runtime::Client, ghl_location_uid: &str) -> crate::runtime::ApiResult<GhlLocation> {
  let mut __path = String::from("/ghl/{ghl_location_uid}/link");
  __path = __path.replace("{ghl_location_uid}", &crate::runtime::encode_path(ghl_location_uid));
  __client.request(crate::runtime::Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
