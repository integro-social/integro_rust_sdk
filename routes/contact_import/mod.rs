// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::types::contact_import::contact_import_detail::ContactImportDetail;
use crate::types::domain::contact_import_row::ContactImportRow;
use crate::types::contact_import::list_import_rows_query::ListImportRowsQuery;
use crate::types::contact_import::list_imports_query::ListImportsQuery;
use crate::types::contact_import::update_contact_import_request::UpdateContactImportRequest;
use crate::types::contact_import::update_import_row_request::UpdateImportRowRequest;
use crate::types::contact_import::upload_contact_import_form::UploadContactImportForm;

/// Cancel the import: every still-queued row becomes `skipped`. Terminal;
/// conversations already created stay.
///
/// Requires `ImportContacts` in the import's group.
pub async fn cancel(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}/cancel");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Delete an import and its rows. Refused while loading — pause or cancel
/// first. Conversations already created stay.
///
/// Requires `ImportContacts` in the import's group.
pub async fn delete(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/contact-import/{import_uid}");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// Drop a staged row instead of fixing it: the record simply leaves the
/// import.
///
/// Requires `ImportContacts` in the import's group.
pub async fn delete_row(__client: &crate::runtime::Client, import_uid: &str, row_id: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/contact-import/{import_uid}/row/{row_id}");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __path = __path.replace("{row_id}", &crate::runtime::encode_path(row_id));
  __client.request(crate::runtime::Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// One import with its derived counters and drain estimate — the progress
/// page's poll.
///
/// Requires `ImportContacts` in the import's group.
pub async fn get(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// List imports, newest first, optionally one group's.
///
/// Requires `ImportContacts`; the list covers only imports of groups where the caller holds it.
pub async fn list(__client: &crate::runtime::Client, __query: &ListImportsQuery) -> crate::runtime::ApiResult<Vec<ContactImportDetail>> {
  let mut __path = String::from("/contact-import");
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Download the file as it was parsed: the original columns and cells, one
/// line per record still in the import — rows deleted while staged are not
/// in it. Cells are re-quoted per RFC 4180, so byte-for-byte it may differ
/// from the upload.
///
/// Requires `ImportContacts` in the import's group.
pub async fn original(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<Vec<u8>> {
  let mut __path = String::from("/contact-import/{import_uid}/original.csv");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request_bytes(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// Pause loading: queued rows stay queued and the loaders skip them until a
/// resume. Rows already being checked finish.
///
/// Requires `ImportContacts` in the import's group.
pub async fn pause(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}/pause");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Download the import's report: the original columns plus the normalized
/// phone, the verdict and its detail, one line per record.
///
/// Requires `ImportContacts` in the import's group.
pub async fn report(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<Vec<u8>> {
  let mut __path = String::from("/contact-import/{import_uid}/report.csv");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request_bytes(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// Resume a paused import: the loaders pick the queue back up where it stood.
///
/// Requires `ImportContacts` in the import's group.
pub async fn resume(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}/resume");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// A page of the import's rows in file order, optionally one verdict only —
/// the fix table and the verdict drill-downs.
///
/// Requires `ImportContacts` in the import's group.
pub async fn rows(__client: &crate::runtime::Client, import_uid: &str, __query: &ListImportRowsQuery) -> crate::runtime::ApiResult<Vec<ContactImportRow>> {
  let mut __path = String::from("/contact-import/{import_uid}/row");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Start loading: pending rows are dealt round-robin across the accounts and
/// one paced loader per account begins verifying numbers and creating
/// conversations. Rows are no longer editable.
///
/// Requires `ImportContacts` in the import's group.
pub async fn start(__client: &crate::runtime::Client, import_uid: &str) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}/start");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Rename the import. Any status.
///
/// Requires `ImportContacts` in the import's group.
pub async fn update(__client: &crate::runtime::Client, import_uid: &str, __body: &UpdateContactImportRequest) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import/{import_uid}");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __client.request(crate::runtime::Method::PUT, &__path, None::<&()>, Some(__body)).await
}
/// Fix a staged row: the corrected values are re-parsed and the row gets its
/// new verdict. Only parse-phase verdicts are editable, and only while the
/// import is staged.
///
/// Requires `ImportContacts` in the import's group.
pub async fn update_row(__client: &crate::runtime::Client, import_uid: &str, row_id: &str, __body: &UpdateImportRowRequest) -> crate::runtime::ApiResult<ContactImportRow> {
  let mut __path = String::from("/contact-import/{import_uid}/row/{row_id}");
  __path = __path.replace("{import_uid}", &crate::runtime::encode_path(import_uid));
  __path = __path.replace("{row_id}", &crate::runtime::encode_path(row_id));
  __client.request(crate::runtime::Method::PUT, &__path, None::<&()>, Some(__body)).await
}
/// Upload a CSV of contacts: the file is parsed synchronously, every record
/// gets a parse verdict, and the import is created `staged` — nothing touches
/// a platform until `start`. Invalid rows stay editable while staged.
///
/// Requires `ImportContacts` in the accounts' group; every account must be a native whatsapp account of that same group.
pub async fn upload(__client: &crate::runtime::Client, __form: reqwest::multipart::Form) -> crate::runtime::ApiResult<ContactImportDetail> {
  let mut __path = String::from("/contact-import");
  __client.request_multipart(crate::runtime::Method::POST, &__path, None::<&()>, __form).await
}
