// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::types::insight::conversation_insight_query::ConversationInsightQuery;
use crate::types::insight::conversation_insight_response::ConversationInsightResponse;
use crate::types::insight::insight_query_request::InsightQueryRequest;
use crate::types::insight::insight_query_response::InsightQueryResponse;
use crate::types::insight::posting_insight_query::PostingInsightQuery;
use crate::types::insight::posting_insight_response::PostingInsightResponse;

/// Customer service over a scope per hour, day, week or month. A customer
/// message opens a request; later customer messages join it until the account
/// replies, or until the customer writes again after 24 hours without a reply.
/// The wait runs from the request's first message to the account's next
/// message, whoever or whatever sent it; a request never answered is counted
/// only as an atendimento. A request opens an atendimento
/// when the customer writes first or after 24 hours of silence. Requests and
/// outreach belong to the period they started in. New conversations count each conversation's first
/// message ever. Periods align to `utc_offset_minutes`; weeks start on
/// Monday.
///
/// Requires `ViewMessages`; covers only accounts of groups where the caller holds it, narrowed by `group_uids`, `social_account_uids` (each must be in scope) and `channels`. Hourly periods over at most 31 days, the others over at most 731 (400 beyond).
pub async fn conversation(__client: &crate::runtime::Client, __body: &ConversationInsightQuery) -> crate::runtime::ApiResult<ConversationInsightResponse> {
  let mut __path = String::from("/insight/conversation");
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Platform posts published over a scope per hour, day, week or month,
/// counted at their publication time; posts removed from the platform are
/// left out. Periods align to `utc_offset_minutes`; weeks start on Monday.
///
/// Requires `ViewPosts`; covers only accounts of groups where the caller holds it, narrowed by `group_uids`, `social_account_uids` (each must be in scope) and `channels`. Hourly periods over at most 31 days, the others over at most 731 (400 beyond).
pub async fn posting(__client: &crate::runtime::Client, __body: &PostingInsightQuery) -> crate::runtime::ApiResult<PostingInsightResponse> {
  let mut __path = String::from("/insight/posting");
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Aggregate collected metrics over many accounts of one channel, by day,
/// week or month. Only accounts of the declared channel take part; a named
/// account of another channel is refused. Breakdown metrics (demographics)
/// cannot be aggregated.
///
/// Requires `ViewInsights` in every group the query reaches; accounts the caller may not see are refused.
pub async fn query(__client: &crate::runtime::Client, __body: &InsightQueryRequest) -> crate::runtime::ApiResult<InsightQueryResponse> {
  let mut __path = String::from("/insight/query");
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
