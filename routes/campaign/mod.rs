// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::types::campaign::audience_request::AudienceRequest;
use crate::types::campaign::campaign_detail::CampaignDetail;
use crate::types::domain::campaign_recipient::CampaignRecipient;
use crate::types::campaign::campaign_stats::CampaignStats;
use crate::types::campaign::create_campaign_request::CreateCampaignRequest;
use crate::types::campaign::list_campaigns_query::ListCampaignsQuery;
use crate::types::campaign::list_recipients_query::ListRecipientsQuery;
use crate::types::domain::message_with_context::MessageWithContext;
use crate::types::campaign::preflight_report::PreflightReport;
use crate::types::campaign::remove_recipients_request::RemoveRecipientsRequest;
use crate::types::campaign::set_accounts_request::SetAccountsRequest;
use crate::types::campaign::test_send_request::TestSendRequest;
use crate::types::campaign::update_campaign_request::UpdateCampaignRequest;

/// Append an audience to the end of the queue — the same sources as a
/// build, added to what is there. Draft, paused or done; conversations
/// already in the ledger (queued or reached) are skipped.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn add_recipients(__client: &crate::runtime::Client, campaign_uid: &str, __body: &AudienceRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/recipient");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Build (or rebuild) the audience: the source resolves to numbers, each
/// assigned an account (balanced, preferring one that already has the
/// conversation), frozen as the recipient ledger. Draft only — past a start
/// the ledger is the record of who was reached; use `addRecipients`.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn build_audience(__client: &crate::runtime::Client, campaign_uid: &str, __body: &AudienceRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/audience");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Cancel the campaign: queued recipients become `canceled`, and campaign
/// messages still waiting in the send queue are pulled back. Terminal;
/// messages already sent stay.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn cancel(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/cancel");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Create a draft campaign in a group. Its accounts are the group's whatsapp
/// accounts (native or Stevo, mixed freely); nothing sends until templates,
/// accounts and an audience exist and `start` passes the preflight.
///
/// Requires `ManageCampaigns` in the target group; group-scoped API keys create into their own group, others must name it.
pub async fn create(__client: &crate::runtime::Client, __body: &CreateCampaignRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign");
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Delete a campaign and its ledger. Refused while running — pause or cancel
/// first. Messages already sent stay in their conversations.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn delete(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/campaign/{campaign_uid}");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// One campaign with its derived counters — the page's poll.
///
/// Requires `ViewCampaigns` in the campaign's group.
pub async fn get(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// List campaigns, newest first, optionally one group's.
///
/// Requires `ViewCampaigns`; the list covers only campaigns of groups where the caller holds it.
pub async fn list(__client: &crate::runtime::Client, __query: &ListCampaignsQuery) -> crate::runtime::ApiResult<Vec<CampaignDetail>> {
  let mut __path = String::from("/campaign");
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Pause the drip: queued recipients stay queued, nothing more is sent until
/// a resume. Messages already in the send queue still go out.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn pause(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/pause");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// The start gate's dry run: hard errors, foreseeable skips, and the sending
/// estimate.
///
/// Requires `ViewCampaigns` in the campaign's group.
pub async fn preflight(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<PreflightReport> {
  let mut __path = String::from("/campaign/{campaign_uid}/preflight");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// A page of the recipient ledger in drip order, optionally one status.
///
/// Requires `ViewCampaigns` in the campaign's group.
pub async fn recipients(__client: &crate::runtime::Client, campaign_uid: &str, __query: &ListRecipientsQuery) -> crate::runtime::ApiResult<Vec<CampaignRecipient>> {
  let mut __path = String::from("/campaign/{campaign_uid}/recipient");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::GET, &__path, Some(__query), None::<&()>).await
}
/// Hand the whole queue out again across the current accounts: balanced,
/// preferring an account that already has each number's conversation. For
/// after accounts were added, or a queue that grew lopsided. Draft, paused
/// and done only.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn redistribute(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/redistribute");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Drop queued recipients from the audience. Draft, paused or done; rows that
/// already ran stay as history.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn remove_recipients(__client: &crate::runtime::Client, campaign_uid: &str, __body: &RemoveRecipientsRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/recipient/remove");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Resume a paused drip where it stood.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn resume(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/resume");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Replace the participating account set. Draft, paused and done only; every
/// account must be a whatsapp (native or Stevo) account of the campaign's
/// group. Queued recipients of an account taken out are handed to the
/// accounts that remain, balanced and preferring one that already knows
/// them; rows that already ran stay. The set may be empty — the queue then
/// waits for the next account change — and the campaign cannot start until
/// it has one.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn set_accounts(__client: &crate::runtime::Client, campaign_uid: &str, __body: &SetAccountsRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/account");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::PUT, &__path, None::<&()>, Some(__body)).await
}
/// Start the drip, from draft or from done — a done campaign restarts on
/// whatever is still queued, never on who was already reached. Runs the
/// preflight and refuses on any error; from then on the dispatcher sends
/// one message per interval inside the window.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn start(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}/start");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// Delivery truth: recipient status crossed with each message's own platform
/// status, plus the reply count.
///
/// Requires `ViewCampaigns` in the campaign's group.
pub async fn stats(__client: &crate::runtime::Client, campaign_uid: &str) -> crate::runtime::ApiResult<CampaignStats> {
  let mut __path = String::from("/campaign/{campaign_uid}/stats");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// Send one template to one conversation, now, outside any schedule — the
/// operator's own eyes on the rendered result.
///
/// Requires `ManageCampaigns` in the campaign's group; the conversation must belong to the same group and channel.
pub async fn test(__client: &crate::runtime::Client, campaign_uid: &str, __body: &TestSendRequest) -> crate::runtime::ApiResult<MessageWithContext> {
  let mut __path = String::from("/campaign/{campaign_uid}/test");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Replace the campaign's schedule, ceilings and rotation. Draft, paused and
/// done only — a running drip is retuned by pausing first.
///
/// Requires `ManageCampaigns` in the campaign's group.
pub async fn update(__client: &crate::runtime::Client, campaign_uid: &str, __body: &UpdateCampaignRequest) -> crate::runtime::ApiResult<CampaignDetail> {
  let mut __path = String::from("/campaign/{campaign_uid}");
  __path = __path.replace("{campaign_uid}", &crate::runtime::encode_path(campaign_uid));
  __client.request(crate::runtime::Method::PUT, &__path, None::<&()>, Some(__body)).await
}
