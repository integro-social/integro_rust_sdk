// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::runtime::{Client, Method, ApiResult, WsConnection, WsHandlers, encode_path};
use crate::types::group::create_group_form::CreateGroupForm;
use crate::types::group::create_group_response::CreateGroupResponse;
use crate::types::group::group_query::GroupQuery;
use crate::types::group::group_response::GroupResponse;
use crate::types::group::set_group_enabled_request::SetGroupEnabledRequest;
use crate::types::group::set_group_logo_form::SetGroupLogoForm;
use crate::types::group::update_group_request::UpdateGroupRequest;

/// Count groups.
///
/// Requires `ViewGroups`; the count covers only groups where the caller holds it.
pub async fn count(client: &Client) -> ApiResult<u64> {
  let mut __path = String::from("/group/count");
  client.request(Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// Create a group, optionally with a logo image.
///
/// Requires `CreateGroups`, which is only ever held platform-wide — no group exists yet to scope it to.
pub async fn create(client: &Client, form: reqwest::multipart::Form) -> ApiResult<CreateGroupResponse> {
  let mut __path = String::from("/group");
  client.request_multipart(Method::POST, &__path, None::<&()>, form).await
}
/// Delete a group that has no API keys and no social accounts.
///
/// Requires `DeleteGroups` in the group itself; fails while the group still has API keys or social accounts.
pub async fn delete(client: &Client, group_uid: &str) -> ApiResult<()> {
  let mut __path = String::from("/group/{group_uid}");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request(Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// Fetch a single group, optionally enriched with per-group counts.
///
/// Requires `ViewGroups` in the group itself; each count is filled only when the caller also holds that count's own permission there — `ViewApiKeys` for `api_key_count`, `ViewMembers` for `member_count` — and is `null` otherwise.
pub async fn get(client: &Client, group_uid: &str, query: &GroupQuery) -> ApiResult<GroupResponse> {
  let mut __path = String::from("/group/{group_uid}");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request(Method::GET, &__path, Some(query), None::<&()>).await
}
/// Serve a group's logo image.
///
/// Requires `ViewGroups` in the group itself.
pub async fn get_logo(client: &Client, group_uid: &str) -> ApiResult<Vec<u8>> {
  let mut __path = String::from("/group/{group_uid}/logo");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request_bytes(Method::GET, &__path, None::<&()>, None::<&()>).await
}
/// List groups, optionally enriched with per-group counts.
///
/// Requires `ViewGroups`; the list covers only groups where the caller holds it, and each count is filled only for those where it also holds that count's own permission — `ViewApiKeys` for `api_key_count`, `ViewMembers` for `member_count` — `null` everywhere else.
pub async fn list(client: &Client, query: &GroupQuery) -> ApiResult<Vec<GroupResponse>> {
  let mut __path = String::from("/group");
  client.request(Method::GET, &__path, Some(query), None::<&()>).await
}
/// Remove a group's logo image.
///
/// Requires `UpdateGroups` in the group itself.
pub async fn remove_logo(client: &Client, group_uid: &str) -> ApiResult<()> {
  let mut __path = String::from("/group/{group_uid}/logo");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request(Method::DELETE, &__path, None::<&()>, None::<&()>).await
}
/// Enable or disable a group; API keys bound to a disabled group are rejected.
///
/// Requires `ToggleGroupEnabled` in the group itself.
pub async fn set_enabled(client: &Client, group_uid: &str, body: &SetGroupEnabledRequest) -> ApiResult<()> {
  let mut __path = String::from("/group/{group_uid}/enabled");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request(Method::PUT, &__path, None::<&()>, Some(body)).await
}
/// Upload or replace a group's logo image.
///
/// Requires `UpdateGroups` in the group itself.
pub async fn set_logo(client: &Client, group_uid: &str, form: reqwest::multipart::Form) -> ApiResult<()> {
  let mut __path = String::from("/group/{group_uid}/logo");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request_multipart(Method::POST, &__path, None::<&()>, form).await
}
/// Update a group's mutable fields such as its name.
///
/// Requires `UpdateGroups` in the group itself.
pub async fn update(client: &Client, group_uid: &str, body: &UpdateGroupRequest) -> ApiResult<()> {
  let mut __path = String::from("/group/{group_uid}");
  __path = __path.replace("{group_uid}", &encode_path(group_uid));
  client.request(Method::PUT, &__path, None::<&()>, Some(body)).await
}
