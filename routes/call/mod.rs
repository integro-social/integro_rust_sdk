// This file was generated by tapi. Do not edit this file manually.
#![allow(unused_imports, unused_mut, clippy::all)]
use crate::types::database::call::Call;
use crate::types::call::call_client_msg::CallClientMsg;
use crate::types::call::call_server_msg::CallServerMsg;
use crate::types::call::place_call_request::PlaceCallRequest;

/// Answer a ringing call, claiming it for the caller — who is then the only
/// one that may carry its media. A second operator answering the same call
/// gets "chamada já atendida".
///
/// Requires `SendMessages` in the conversation's group.
pub async fn answer(__client: &crate::runtime::Client, conversation_uid: &str, call_uid: &str) -> crate::runtime::ApiResult<Call> {
  let mut __path = String::from("/conversation/{conversation_uid}/call/{call_uid}/answer");
  __path = __path.replace("{conversation_uid}", &crate::runtime::encode_path(conversation_uid));
  __path = __path.replace("{call_uid}", &crate::runtime::encode_path(call_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// End a call in progress. Not restricted to the operator holding it: a call
/// whose operator walked away has to be endable by whoever is still there. A
/// call that was never answered here has nothing to hang up — refusing that one
/// is `reject`.
///
/// Requires `SendMessages` in the conversation's group.
pub async fn hangup(__client: &crate::runtime::Client, conversation_uid: &str, call_uid: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/conversation/{conversation_uid}/call/{call_uid}/hangup");
  __path = __path.replace("{conversation_uid}", &crate::runtime::encode_path(conversation_uid));
  __path = __path.replace("{call_uid}", &crate::runtime::encode_path(call_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// The call's live media, typed both ways. Binary frames carry the media
/// itself — `0x01` plus one 1920-byte s16le 16 kHz mono audio frame, or `0x02`
/// plus a flags byte and one H.264 Annex-B access unit — in exactly the
/// formats WhatsApp uses, so nothing between the browser and the contact
/// transcodes. Text frames carry the in-call controls: the video upgrade
/// handshake, camera state, orientation and reactions. Everything that decides
/// whether the call exists stays on the REST routes above.
///
/// Reconnecting re-attaches and replaces the previous tracks; a claimed call
/// whose operator stays gone for 30 seconds is hung up rather than left
/// running silently.
///
/// Requires `SendMessages` in the conversation's group, and the caller must be
/// the operator who answered (or placed) this call.
pub fn media(__client: &crate::runtime::Client, conversation_uid: &str, call_uid: &str, __handlers: crate::runtime::WsHandlers<CallServerMsg>) -> crate::runtime::WsConnection<CallClientMsg> {
  let mut __path = String::from("/conversation/{conversation_uid}/call/{call_uid}/media/ws");
  __path = __path.replace("{conversation_uid}", &crate::runtime::encode_path(conversation_uid));
  __path = __path.replace("{call_uid}", &crate::runtime::encode_path(call_uid));
  __client.ws(&__path, None::<&()>, __handlers)
}
/// Call the conversation's contact. The body is channel-tagged — only
/// whatsapp_native carries calls, so no other channel's shape deserializes.
/// Returns as soon as the offer is on the wire — the contact's phone has not
/// rung yet, and the phases that follow arrive as `call_updated` events. Only
/// one call at a time per account.
///
/// Requires `SendMessages` in the conversation's group.
pub async fn place(__client: &crate::runtime::Client, conversation_uid: &str, __body: &PlaceCallRequest) -> crate::runtime::ApiResult<Call> {
  let mut __path = String::from("/conversation/{conversation_uid}/call");
  __path = __path.replace("{conversation_uid}", &crate::runtime::encode_path(conversation_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, Some(__body)).await
}
/// Refuse a ringing call. Available to anyone who could have answered it — a
/// shared inbox's ringing call is everyone's to decline.
///
/// Requires `SendMessages` in the conversation's group.
pub async fn reject(__client: &crate::runtime::Client, conversation_uid: &str, call_uid: &str) -> crate::runtime::ApiResult<()> {
  let mut __path = String::from("/conversation/{conversation_uid}/call/{call_uid}/reject");
  __path = __path.replace("{conversation_uid}", &crate::runtime::encode_path(conversation_uid));
  __path = __path.replace("{call_uid}", &crate::runtime::encode_path(call_uid));
  __client.request(crate::runtime::Method::POST, &__path, None::<&()>, None::<&()>).await
}
/// List the calls ringing right now on accounts the caller can see — inbound,
/// unclaimed and still answerable. Ring events are broadcast once and never
/// replayed, so a client that just connected (a reloaded page, a login while a
/// call is ringing) hydrates its ringing list from here.
///
/// Requires `ViewMessages`; the list is clamped to the groups where it is held.
pub async fn ringing(__client: &crate::runtime::Client) -> crate::runtime::ApiResult<Vec<Call>> {
  let mut __path = String::from("/call/ringing");
  __client.request(crate::runtime::Method::GET, &__path, None::<&()>, None::<&()>).await
}
